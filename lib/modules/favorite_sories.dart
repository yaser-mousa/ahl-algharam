import 'package:flutter/material.dart';

class FavoriteLoveStories extends StatelessWidget {
  const FavoriteLoveStories({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('FavoriteLoveStories'),),
    );
  }
}
